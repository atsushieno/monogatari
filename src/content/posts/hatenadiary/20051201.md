---
title: "2005-12-01"
pubDatetime: 2005-12-01T00:00:00+09:00
author: atsushieno
timezone: "Asia/Tokyo"
description: "げいのうた どうでもいいけど何で僕が見るTVドラマってみんな視聴率低いのかしらん。 canvas/SVG support in firefox 1.5 tao/mozillaﾊｶｰvladのblog経由で知ったのだけど、firefox 1.5では新しくcanvas要素が使えるらしい。 Canvas Painter..."
canonicalURL: "https://atsushieno.hatenadiary.org/20051201"
---

## げいのうた

どうでもいいけど何で僕が見るTVドラマってみんな視聴率低いのかしらん。

## canvas/SVG support in firefox 1.5

tao/mozillaﾊｶｰvladのblog経由で知ったのだけど、firefox 1.5では新しくcanvas要素が使えるらしい。

- [Canvas Painter](http://caimansys.com/painter/index.html)
- [Canvascape 3D walker](http://www.abrahamjoffe.com.au/ben/canvascape/)
- [Tab preview extension](http://ted.mielczarek.org/code/mozilla/tabpreview/)
- [foXpose](http://viamatic.com/firefox/)

それから、SVGサポートも重要なアップデートのひとつで、[コレ](http://isthis4real.com/orbit.xml)なんかは、xhtml中にsvgがダイレクトに埋め込まれていたりする。

ｶｺｲｲ!

追記: 日本でもお遊びJavaScriptの権威(どういう表現だ)の高橋さんが[グラフ](http://jsgt.org/mt/archives/01/000514.html)を描くアプリケーションを作ってますね。実態は[こちら](http://nanto.asablo.jp/blog/2005/09/27/89628)みたい。素晴らしぃ。firefoxのXTFについて情報を探していて偶然見つけますた。

## MWF feature completed for mono 1.2

MWFのMono 1.2でリリースする予定の機能がついに完成したらしい。あとは（現時点の見積もりで）3ヶ月程度のバグフィックス期間ということになる。

というわけで、[ここ](http://tirania.org/blog//texts/mono-status.html#winforms)で書かれている見送りの機能[^7af2cea4]以外で、何か気付いたらbugzillaに投げてやって下さい。ここにコメント書くなりTBするなりしてもらってもかまいません。あ、当然ながらwin32のdllをexternで見に行くようなコンポーネントは動きません。

[^7af2cea4]: OS XサポートとかダブルバッファリングとかIMEとか。そのうち2chのmonoスレの>>130が翻訳する予定w

## from array to fixed pointer, and ABCREM

例のEncoding実装の話の続き。

現在、とりあえずI18Nライブラリに含まれるカスタムエンコーディングのGetBytes()[^d03d1ebc]を例のポインタベースで書き換えようとしているのだけど、ひとつ問題があることに気付いた。ABCREM（配列境界チェック）はmanaged arrayには有効だけど、fixed pointerには効かない:

```
using System;
```

public class Test  
{  
public unsafe static void Main (string [] args)  
{  
int count = 10000;  
if (args.Length > 0)  
count = int.Parse (args [0]);

char [] arr = new char [100];  
int len = arr.Length;  
char c;  
fixed (char\* cptr = arr) {  
//char [] cptr = arr;  
DateTime start = DateTime.Now;  
for (int i = 0; i < count; i++)  
for (int x = 0; x < len; x++)  
c = cptr [x];  
Console.WriteLine (  
DateTime.Now.Ticks - start.Ticks);  
}  
}  
}

ということは、単純にGetBytes(char,int,int,byte,int)をGetBytes(char\*,int,byte\*,int)に置き換えると、（ABCREMが有効に機能するという前提で）パフォーマンスがABCREMの分だけ損なわれるということにもなりうる。

Massiに聞いてみたら、fixed pointerについては、（境界が無いので）ABCはあり得ないけど、null checkについては可能だろう、という話で、いずれHSSAが実装された暁には、この最適化もサポートしてもらえそうだ。ちなみに、MS.NETで↑のコードを動かしてみたら、1.xではfixed pointerの方が遅かったが、2.0では逆の結果が出た。

たぶん、null checkが有効になるようなコードは、現在のI18Nライブラリみたいな実装になっていると不可能だろうから、この辺をいじるのは後にした方が良いかもしれないが、どっちにしろ現在のI18NコードではABCREMも効いていないはずなので、とりあえずこのまま進めてしまおう。

[^d03d1ebc]: こっちだけ。GetChars()は先日書いた事情でポインタに出来ない
